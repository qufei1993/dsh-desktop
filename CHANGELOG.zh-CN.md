[English](CHANGELOG.md) | 简体中文

# 变更日志

本文件记录 DSH Desktop 的重要变化。版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## 未发布

## [0.2.4] - 2026-08-22

### 修复

- 插件命令现在通过由内置 Node.js 运行时支持的 Desktop 自有启动器，解析到当前选中的官方 DSH CLI。这避免了对全局 `dsh` 安装以及包管理器 shim 的依赖；后者在精确版本安装、迁移或重新安装后可能仍指向已删除的临时目录。

## [0.2.3] - 2026-08-21

### 修复

- DSH 版本安装现在会把内置 Node.js 目录加入 `PATH`，确保从 Finder 或资源管理器启动、没有开发者终端环境时，依赖生命周期脚本仍能正常执行。安装失败时还会保留详细诊断日志，不再只显示被截断的进度文本。

## [0.2.2] - 2026-08-21

### 新增

- 版本管理现可单击卸载用户安装的 DSH 版本；随应用提供的内置版本和当前选中的版本会受到保护，无法卸载，已卸载版本仍可重新安装。

### 修复

- DSH 版本安装现在仅允许运行官方 subprocess helper 的轻量权限修复脚本。原生依赖会直接使用包内对应平台的预编译文件，不再尝试本机 `node-gyp` 或 CMake 构建，因此安装新发布的 DSH 版本不再依赖开发工具链。

## [0.2.1] - 2026-08-21

### 修复

- 使用内置 pnpm 内容寻址存储替代 npm 在大型依赖树上的病态慢速版本安装，同时保留官方依赖的构建脚本。安装现可复用已下载包、显示依赖进度、在连续五分钟无输出时终止，并会在下次启动时清理中断留下的临时目录。
- 安装官方 DSH 版本前刷新 npm registry 元数据，避免新发布版本因本地元数据过期而安装失败。
- 以官方目录中最高的有效包版本判定最新 DSH，不再依赖可能滞后的 npm `latest` 标签。
- 启动 DSH 0.1.0-rc.8 及后续版本时传入 `--no-open`，仅在桌面窗口中打开官方 Web UI，不再同时打开系统浏览器。
- 随包提供 pnpm 11.22.0，并仅向官方 DSH 子进程暴露内置 Node.js 与 pnpm 命令，确保应用在精简 GUI `PATH` 下仍可执行插件操作。未经授权的依赖构建脚本仍会被阻止，但不会再让原本有效的插件更新以 `ERR_PNPM_IGNORED_BUILDS` 失败。

## [0.2.0] - 2026-08-16

### 新增

- 支持检测更新的官方 DSH 包版本；在官方 DSH 窗口显示可关闭的更新提醒，并可从版本管理页直接更新。

## [0.1.0] - 2026-08-15

### 新增

- macOS Apple Silicon、macOS Intel 与 Windows x64 桌面应用。
- 随包 Node.js 24 LTS 与经过校验的官方 DSH 版本。
- npm 官方 DSH 全版本查询、安装、保留和切换。
- 启动时直达官方 DSH 页面，版本管理从系统菜单进入。
- DSH Desktop 通过 GitHub Releases 检查更新：macOS 手动下载，Windows 可在应用内安装。
- 自动跟随环境变量、系统代理或本机 `127.0.0.1:7890` 代理。
- 原生“关于 DSH Desktop”、版本管理菜单图标和右上角应用更新入口。
- 简体中文与 English 界面，可跟随系统语言或记住用户选择。

### 变更

- 发布包仅保留目标平台所需的 DSH 原生依赖，移除 Windows 调试符号并启用最高压缩，以减小下载体积。
- Release 下载列表收敛为三个平台安装包；Windows 仅保留自动更新所需的更新清单。

### 安全

- DSH 页面使用无 preload 的沙箱窗口，不暴露 Node.js 或 Electron API。
- DSH 本地服务地址限制为 `127.0.0.1`。
- 资源校验、精确版本安装和产品边界自动审计。
- 明确采用不购买平台证书的社区构建，macOS 使用 ad-hoc 签名。
